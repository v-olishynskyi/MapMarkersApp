/**
 * @namespace Settings
 * @category 
 * @subcategory 
 *  */
import React from 'react';
import useStyles from './styles';
import {SettingsProps} from './types';

/**
 * Settings
 * 
 *
 * @memberof 
 * @param {SettingsProps} params
 *
 * @example
 * // How to use Settings:
 *  <Settings />
 */
const Settings: React.FC<SettingsProps> = () => {
  const styles = useStyles();

  return <></>;
};

export default Settings;
