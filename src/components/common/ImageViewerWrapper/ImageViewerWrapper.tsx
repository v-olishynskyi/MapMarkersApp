/**
 * @namespace ImageViewerWrapper
 * @category 
 * @subcategory 
 *  */
import React from 'react';
import useStyles from './styles';
import {ImageViewerWrapperProps} from './types';

/**
 * ImageViewerWrapper
 * 
 * @memberof 
 * @param {ImageViewerWrapperProps} params
 *
 * @example
 * // How to use ImageViewerWrapper:
 *  <ImageViewerWrapper />
 */
const ImageViewerWrapper: React.FC<ImageViewerWrapperProps> = () => {
  const styles = useStyles();

  return <></>;
};

export default ImageViewerWrapper;
