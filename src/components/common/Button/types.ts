import type { ButtonProps as RNUILibButtonProps } from 'react-native-ui-lib';

/**
 * @memberof Button
 * @typedef {Object} ButtonProps
 */
export type ButtonProps = {
  loading?: boolean;
} & RNUILibButtonProps;
