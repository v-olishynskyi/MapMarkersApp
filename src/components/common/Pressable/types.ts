import { PropsWithChildren } from 'react';
import { PressableProps as RNPressableProps } from 'react-native';

/**
 * @memberof Pressable
 * @typedef {Object} PressableProps
 */
export type PressableProps = PropsWithChildren<RNPressableProps>;
